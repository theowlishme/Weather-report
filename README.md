# Weather Application (Python)

This is a basic Weather Application.

The user enters a **city name**, and the program shows:

- Temperature (°C)
The app uses the **OpenWeatherMap API** to get real weather data.

## What the project does

1. Ask the user to type a city name  
2. Send a request to OpenWeatherMap  
3. Receive the weather data in JSON format  
4. Extract temperature 
5. Display the result

## How the program works 

1. uses python languages and library
2. uses Open weather app website's API
3.  program call the api and shows the information
