# Weather App (Python)

This is a basic weather application
The user enters a city name and the program shows the current temperature using the OpenWeatherMap API.

## Used

- Python
- OpenWeatherMap API key
- `requests` library
